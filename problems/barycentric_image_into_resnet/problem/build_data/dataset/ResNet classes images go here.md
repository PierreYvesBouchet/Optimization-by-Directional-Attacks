However, the dataset of images used in the problem must be downloaded manually. It is available at [this link](https://github.com/EliSchwartz/imagenet-sample-images). After downloading the 1000 images, set them (the images directly, not the .zip file) at the location
```
problems/
	barycentric_image_into_resnet/
		problem/
			build_data/
				dataset/
```